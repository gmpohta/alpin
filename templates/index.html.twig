<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Symfony + Vue.js Hybrid</title>
    
    <!-- Подключение стилей Symfony -->
    <link rel="stylesheet" href="{{ asset('build/app.css') }}">
    
    <style>
        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }
        .symfony-section {
            background: #f8f9fa;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 20px;
            border-left: 4px solid #6f42c1;
        }
        .hybrid-section {
            background: #e8f4fd;
            padding: 20px;
            border-radius: 8px;
            margin: 20px 0;
            border-left: 4px solid #007bff;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>🔀 Symfony + Vue.js Hybrid Application</h1>
        
        <div class="symfony-section">
            <h2>🔄 Symfony Server Data</h2>
            <p><strong>Current Time:</strong> {{ server_data.currentTime|date('Y-m-d H:i:s') }}</p>
            <p><strong>PHP Version:</strong> {{ server_data.phpVersion }}</p>
            <p><strong>Symfony Version:</strong> {{ server_data.symfonyVersion }}</p>
            
            <h3>Users from Symfony:</h3>
            <ul>
                {% for user in server_data.users %}
                    <li>{{ user.name }} ({{ user.role }})</li>
                {% endfor %}
            </ul>
        </div>
        
        <div class="hybrid-section">
            <h2>🎯 Vue.js Components Integrated in Symfony</h2>

            <div data-vue-app="Counter"></div>
            <div data-vue-app="UserList" data-initial-users="{{ users|json_encode|e('html_attr') }}"></div>
        </div>
        

        <div class="symfony-section">
            <h3>🔗 Navigation</h3>
            <a href="{{ path('index') }}" class="btn btn-primary">Home</a>
            <a href="{{ path('vue_page') }}" class="btn btn-secondary">Vue.js Page</a>
        </div>
    </div>

    {{ encore_entry_script_tags('vue_components') }}
</body>
</html>